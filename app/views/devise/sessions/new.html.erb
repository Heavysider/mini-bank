<div class="card mx-auto mt-5 px-4 py-4" style="width: 20rem;">
  <h2>Log in</h2>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="mb-3">
      <%= f.label :email, class: 'form-label', for: 'exampleInputEmail1' %><br />
      <%= f.email_field :email, class: "form-control", id: "exampleInputEmail1", autofocus: true %>
    </div>

    <div class="mb-3">
      <%= f.label :password, class: 'form-label', for: 'exampleInputPassword1' %><br />
      <%= f.password_field :password, class: "form-control", id: "exampleInputPassword1" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="mb-3 form-check">
        <%= f.check_box :remember_me, class: "form-check-input", id: "exampleCheck1" %>
        <%= f.label :remember_me, class: "form-check-label", for: "exampleCheck1" %>
      </div>
    <% end %>

    <div class="actions">
      <%= f.submit "Log in", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
